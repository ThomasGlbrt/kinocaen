{% extends 'base.html.twig' %}
{% block title %}Modification du profil {% endblock %}
{% block contenu %}
<div class="container">
    <div class="row">
        <div class="col-md-8">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
        <div class="col-md-4">
        </div>
    </div>
</div>
{% endblock %}